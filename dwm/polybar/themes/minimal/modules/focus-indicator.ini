[module/focus-indicator]
type = custom/script
; Read the focus display file every 500ms
exec = cat /tmp/polybar-focus/${MONITOR}-display 2>/dev/null
interval = 0.5

; Visual indicator - colors are in the file content
label = %output%
format = <label>
format-padding = 0
